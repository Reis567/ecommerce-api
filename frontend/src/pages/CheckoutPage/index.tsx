

import React from 'react';
import { CheckoutContainer, CheckoutTitle } from './index.styles.tsx';

const CheckoutPage: React.FC = () => {
  return (
    <CheckoutContainer>
      <CheckoutTitle>Checkout</CheckoutTitle>
      {/* Adicione o conteúdo da página de checkout aqui */}
    </CheckoutContainer>
  );
};

export default CheckoutPage;
